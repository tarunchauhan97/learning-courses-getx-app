import 'package:flutter/material.dart';

class MyCoursesCard extends StatelessWidget {
  const MyCoursesCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
